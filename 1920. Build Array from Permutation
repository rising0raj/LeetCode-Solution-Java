class Solution {
    public int[] buildArray(int[] nums) {
        int a=nums.length;
        int b[]=new int[a];
        for(int i=0;i<nums.length;i++){
            b[i]=nums[nums[i]];

        }
        return b;
    }
}
